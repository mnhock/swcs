package swcs.converter.after;

import java.io.File;

final class JpgCompression implements Compression {

    @Override
    public File compress(File source) {
        File compressedFile = null;
        // do compression on file;

        return compressedFile;
    }
}